# Batch: GitHub issues #59, #60 (2026-09-17)

Scope note: no new public contract; no architectural change. Both issues were filed by the
engineer in July 2026 and re-validated against `main` today (#59 still reproduces; #60's
multi-version hypothesis is not supported by the registry on disk).

## Findings

1. **#59 — `hooks/scope-guard.sh` anchors to shipped, archived sidecars** (and can pick
   `docs/specs/baseline/*.json`, a snapshot, as the spec). Behavioral. Skip archived slugs
   using `baseline/*.audit.jsonl`; `-maxdepth 1` on the candidate search. Test:
   `tests/hooks/test-scope-guard-archived.sh`.
2. **#60 — Stop hooks appear to fire multiple times.** Diagnosis: 9 = 5 plugin + 4 project
   registrations; plugin copies exit early via the double-run guard; cache leftovers are
   not extra registrations per `installed_plugins.json`. `mtk-doctor` gains an accounting
   PASS line, an unguarded-double-wiring WARN, and a stale-cache-versions WARN. Test:
   `tests/hooks/test-mtk-doctor-stop-hooks.sh`.

## Post-batch

CHANGELOG `[Unreleased]`; manifest entries; validate-toolkit; full hook suite; comments on
both issues. No version bump.
