{
  "slug": "competitive-borrows-wave1",
  "date": "2026-08-05",
  "scope": "new-feature",
  "open_decisions": [],
  "skill_precedence_gate": "pass",
  "change_manifest": [
    { "path": "scripts/poison-lint.sh", "action": "modify", "purpose": "B0 blocking pre-flight — replace bash-4 mapfile with a portable read loop so validate-toolkit.sh runs on macOS bash 3.2 (engineer-approved at the 2026-08-05 gate)" },
    { "path": "scripts/run-benchmarks.sh", "action": "modify", "purpose": "Per-section pass/fail tracking; fix vacuous validate-toolkit assertion; emit benchmarks/results.json" },
    { "path": "benchmarks/results.json", "action": "create", "purpose": "Published measured results — per-section counts, measures string, explicit excludes array" },
    { "path": "benchmarks/README.md", "action": "create", "purpose": "Methodology: what the numbers measure and what they exclude" },
    { "path": "hooks/rule-trigger.sh", "action": "create", "purpose": "PreToolUse hook — match tool+pattern against rule triggers, deliver source rule just-in-time" },
    { "path": ".claude/references/rule-frontmatter.md", "action": "create", "purpose": "Contract for trigger/strength rule frontmatter. Moved from .claude/rules/ during Batch B: a file under rules/ is picked up by build-rule-index.sh and would pollute the always-on wake-up layer with a meta-document (S1.8 — on-demand docs belong in references/)" },
    { "path": ".claude/rules/git-workflow.md", "action": "modify", "purpose": "Add trigger + strength frontmatter" },
    { "path": ".claude/rules/hooks-and-scripts.md", "action": "modify", "purpose": "Add trigger + strength frontmatter" },
    { "path": ".claude/rules/skill-authoring.md", "action": "modify", "purpose": "Add trigger + strength frontmatter" },
    { "path": ".claude/rules/toolkit-structure.md", "action": "modify", "purpose": "Add trigger + strength frontmatter" },
    { "path": "scripts/build-rule-index.sh", "action": "modify", "purpose": "Emit .claude/rules/triggers.index fast-path index" },
    { "path": ".claude/rules/INDEX.md", "action": "modify", "purpose": "Regenerated by build-rule-index.sh (mechanical)" },
    { "path": ".claude/rules/triggers.index", "action": "create", "purpose": "Generated fast-path index consumed by rule-trigger.sh (mechanical)" },
    { "path": "hooks/hooks.json", "action": "modify", "purpose": "Wire rule-trigger.sh on PreToolUse and SessionStart(compact) re-arm" },
    { "path": ".claude/settings.json", "action": "modify", "purpose": "Same wiring for this dev checkout (S3.6)" },
    { "path": ".claude/references/review-finding-schema.md", "action": "modify", "purpose": "ABSTAINED verdict, abstention object, lane-accounting rule" },
    { "path": ".claude/skills/code-review-and-quality/SKILL.md", "action": "modify", "purpose": "Lane accounting step — abstained/missing lane never contributes to PASS" },
    { "path": ".claude/skills/implement/SKILL.md", "action": "modify", "purpose": "Phase 4 lane-accounting requirement" },
    { "path": ".claude/agents/compliance-reviewer.md", "action": "modify", "purpose": "Emit ABSTAINED verdict in JSON, not prose only" },
    { "path": ".claude/agents/test-reviewer.md", "action": "modify", "purpose": "Emit ABSTAINED verdict in JSON, not prose only" },
    { "path": ".claude/agents/architecture-reviewer.md", "action": "modify", "purpose": "Emit ABSTAINED verdict in JSON, not prose only" },
    { "path": ".claude/agents/silent-failure-hunter.md", "action": "modify", "purpose": "Emit ABSTAINED verdict in JSON, not prose only" },
    { "path": ".claude/agents/context-miner.md", "action": "modify", "purpose": "Emit ABSTAINED verdict in JSON, not prose only" },
    { "path": ".claude/agents/plan-gap-reviewer.md", "action": "modify", "purpose": "Emit ABSTAINED verdict in JSON, not prose only" },
    { "path": "tests/pressure-tests/reviewer-abstention.md", "action": "create", "purpose": "S2.7 adversarial test — failed lane must not read as clean" },
    { "path": ".claude/manifest.json", "action": "modify", "purpose": "Register all new files (C0.2)" }
  ],
  "scope_reductions": [
    { "path": "scripts/validate-toolkit.sh", "reason": "Not needed. validate-toolkit.sh:326-327 already invokes `build-rule-index.sh --check`; extending that script's --check mode to cover triggers.index satisfies S3.10 with no edit to the validator. Dropped from the manifest rather than touched for symmetry." }
  ],
  "public_contracts": [
    { "kind": "schema", "signature": "benchmarks/results.json { generated_at, totals, sections[], measures, excludes[] }", "change": "new", "surface": "internal-tooling" },
    { "kind": "schema", "signature": "rule frontmatter trigger: {tool, pattern, path} + strength: inject|require-read|block", "change": "new", "surface": "internal-tooling" },
    { "kind": "schema", "signature": "review verdict enum gains ABSTAINED + abstention {reason, stage}", "change": "modified", "surface": "internal-tooling" }
  ],
  "success_criteria": [
    { "id": "SC1", "description": "rule-trigger.sh delivers a matching rule's source on a matched tool call and is silent on a non-match", "verification": "bash scripts/run-benchmarks.sh (BENCHMARK 8)", "evidence_channel": "script-output", "observable": "benchmark section 'Rule Trigger' reports pass for both match and non-match cases" },
    { "id": "SC2", "description": "Hook fails open — malformed frontmatter, missing index, or unreadable source never blocks a tool call", "verification": "bash scripts/run-benchmarks.sh (BENCHMARK 8)", "evidence_channel": "script-output", "observable": "exit 0 on corrupted-index and missing-index inputs" },
    { "id": "SC3", "description": "run-benchmarks.sh writes benchmarks/results.json with per-section counts and an explicit excludes array", "verification": "bash scripts/run-benchmarks.sh && cat benchmarks/results.json", "evidence_channel": "script-output", "observable": "file contains sections[] with per-section pass/fail and a non-empty excludes[]" },
    { "id": "SC4", "description": "The validate-toolkit benchmark assertion can actually fail (no longer vacuous)", "verification": "inspect propagated exit code path in run-benchmarks.sh", "evidence_channel": "script-output", "observable": "VAL_EXIT reflects validate-toolkit.sh's real exit status" },
    { "id": "SC5", "description": "A reviewer that cannot complete emits verdict ABSTAINED with a populated abstention.reason", "verification": "tests/pressure-tests/reviewer-abstention.md", "evidence_channel": "manual", "observable": "schema documents ABSTAINED; all six agents instruct emitting it" },
    { "id": "SC6", "description": "Orchestrator accounts for every dispatched lane; abstained or missing lane cannot yield overall PASS", "verification": "tests/pressure-tests/reviewer-abstention.md", "evidence_channel": "manual", "observable": "code-review-and-quality and implement Phase 4 both carry the lane-accounting rule" },
    { "id": "SC7", "description": "Toolkit validation passes with all new files registered", "verification": "bash scripts/validate-toolkit.sh", "evidence_channel": "script-output", "observable": "prints 'Toolkit validation passed' with exit 0" }
  ],
  "test_manifest": [
    { "path": "scripts/run-benchmarks.sh", "covers": ["SC1", "SC2", "SC3", "SC4"] },
    { "path": "tests/pressure-tests/reviewer-abstention.md", "covers": ["SC5", "SC6"] },
    { "path": "scripts/validate-toolkit.sh", "covers": ["SC7"] }
  ],
  "plan": {
    "batches": [
      { "id": "A", "title": "B1 — published benchmark results", "files": ["scripts/run-benchmarks.sh", "benchmarks/results.json", "benchmarks/README.md", ".claude/manifest.json"], "mechanical": false },
      { "id": "B", "title": "B2 — trigger-bound rule delivery", "files": ["hooks/rule-trigger.sh", ".claude/rules/rule-frontmatter.md", ".claude/rules/git-workflow.md", ".claude/rules/hooks-and-scripts.md", ".claude/rules/skill-authoring.md", ".claude/rules/toolkit-structure.md", "scripts/build-rule-index.sh", ".claude/rules/INDEX.md", ".claude/rules/triggers.index", "hooks/hooks.json", ".claude/settings.json", "scripts/validate-toolkit.sh", ".claude/manifest.json"], "mechanical": false },
      { "id": "C", "title": "B3 — reviewer abstention semantics", "files": [".claude/references/review-finding-schema.md", ".claude/skills/code-review-and-quality/SKILL.md", ".claude/skills/implement/SKILL.md", ".claude/agents/compliance-reviewer.md", ".claude/agents/test-reviewer.md", ".claude/agents/architecture-reviewer.md", ".claude/agents/silent-failure-hunter.md", ".claude/agents/context-miner.md", ".claude/agents/plan-gap-reviewer.md", "tests/pressure-tests/reviewer-abstention.md", ".claude/manifest.json"], "mechanical": false }
    ]
  },
  "out_of_scope": [
    "Migrating existing rules from inject to block strength (behavior change; separate spec)",
    "A compile command generating rules from CLAUDE.md (nunchi's full feature)",
    "Retrofitting results.json into scripts/run-evals.sh",
    "Version bump + checksums.sha256 regeneration (separate release commit, S4.11)"
  ],
  "security_impact": "none",
  "blocking_dependency": {
    "path": "scripts/poison-lint.sh",
    "issue": "uses mapfile (bash 4+); macOS ships bash 3.2, so validate-toolkit.sh exits non-zero before any change here",
    "effect": "SC7 and C0.8 cannot be satisfied until fixed or explicitly waived by the engineer",
    "pre_existing": true
  },
  "assumptions": [
    "[VERIFIED:scripts/run-benchmarks.sh:377-386] Runner tracks global PASS/FAIL/TOTAL and calls mtk_record_benchmark_run; per-section attribution does not exist",
    "[VERIFIED:scripts/run-benchmarks.sh:367-369] VAL_EXIT=$? after a command substitution containing '|| true' is always 0 — the validate assertion is vacuous",
    "[VERIFIED:.claude/references/review-finding-schema.md:22] verdict enum is PASS|NEEDS_CHANGES with no abstention channel",
    "[VERIFIED:.claude/agents/test-reviewer.md:115-122] Agents describe BLOCKED/NEEDS_CONTEXT in prose with no schema-valid emit path and no orchestrator consumer",
    "[VERIFIED:.claude/rules/git-workflow.md:1-12] Rule files already carry paths: and axes: frontmatter — trigger: extends an existing schema",
    "[VERIFIED:hooks/hooks.json:26-56] PreToolUse wires three hooks via ${CLAUDE_PLUGIN_ROOT} with the redundant-invocation guard",
    "[VERIFIED:.claude/rules/hooks-and-scripts.md S3.3] jq is not an allowed dependency; python3 is baseline — JSON emission uses python3"
  ],
  "risks": [
    "A PreToolUse hook fires on every matched tool call; a slow or crashing rule-trigger hook degrades every session. Mitigated by fail-open on all error paths, hooks.json timeout, and an index fast path that exits before parsing",
    "B2 changes what enters context on matched tool calls; default strength inject keeps it advisory rather than blocking",
    "Engineer's standing 'no AgentTool unless requested' instruction forbids the HIGH-rigor subagent path and Phase 4 reviewer agents — batches run inline with reduced context isolation. Declared, not silent"
  ]
}
