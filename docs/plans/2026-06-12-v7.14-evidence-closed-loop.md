# Plan — v7.14.0 "Evidence and the Closed Loop"

- **Spec:** `docs/specs/2026-06-12-v7.14-evidence-closed-loop.md` (+ `.json` sidecar)
- **Workflow:** `wf-20260612T125248Z-980e58`
- **Branch:** `feat/v7.14-evidence-closed-loop` (off origin/main @ 11f94fd)
- **Scope:** new-feature · 42 files · 8 batches · security_impact: secrets-change
- **Rigor:** MAX (score 29 — 8 batches +8, 42 files +14, security +3, 7 public contracts +4 cap; hard triggers all met) → subagent path, Stage 2 = test-reviewer + architecture-reviewer + silent-failure-hunter

## Batch graph

B1 (foundation: schemas + 7 references) → B2 (verification skills) → B3 (hooks) → B4 (dispatch contract) → B5 (review wiring) → B6 (lessons loop) → B7 (brainstorming + provenance/package wiring) → B8 (release, checksums last).

B2–B7 all reference B1 assets (F-codes, frames, rubric, schema fields), so B1 is the only hard dependency edge; B2..B7 are pairwise independent but run sequentially to keep drift checks simple. B8 strictly last (S4.11).

## Batches

### B1 — Schemas & references foundation (11 files)
**Files:** handoff.schema.json, workflow-artifact-schema.md, workflow-artifact.sh, validate-handoff.sh, ai-failure-modes.md (new), lesson-mining-rubric.md (new), divergence-frames.md (new), learnings-schema.md, dependency-intake-checklist.md, review-finding-schema.md, security-checklist.md
**Acceptance:** evidence_channel enum {test-run, build-output, http-probe, cli-stdout, db-state-diff, browser, log-capture, script-output} + observable in schema; criteria_status (pending|verified|re-armed) in artifact schema + sh helper; `abandon` subcommand; ai-failure-modes ≥12 `### F`-coded modes each with citation + C# bad/good + severity; rubric has ≥3 reject rules + transcripts-untrusted; frames ≥10 incl. regulator/auditor/fraudster/back-office-quarter-end/3am-on-call; learnings fields optional (back-compat); dependency criterion 0 with per-stack registry commands; review-finding optional failure_mode; security-checklist global-config-self-mutation prohibition. Fixture pair for SC1.
**Boundary:** no skill-file edits in this batch. validate-handoff printf bug fixed in passing (file already in manifest).
**Governing constraints:** C0.2 (manifest later in B8), C0.4, S3.x for sh edits, L-2026-05-08-004.

### B2 — Verification contract skills (4 files)
**Files:** verification-before-completion/SKILL.md, spec-driven-development/SKILL.md, hooks/verify-completion, plan-gap-reviewer.md
**Acceptance:** criterion-by-criterion loop citing observable per channel; "tests alone never prove done" for behavior-shaped changes; re-arm rule (any edit after verification → all criteria re-armed → completion rejected); spec skill gains channel/observable fields in criteria table, dirty-worktree step (git status --porcelain → Risks + out_of_scope), provenance tags [VERIFIED:path]/[ASSUMED]/[CITED:url] in assumptions, trap-register carry-over ("Rejected alternatives"); verify-completion message names re-arm; plan-gap-reviewer rejects plans overlapping dirty paths. Tests: test-verify-completion-rearm.sh + pressure test verification-rearm.md.
**Boundary:** no changes to implement/SKILL.md (B7) or hooks registration (B3).
**Governing constraints:** C0.3 anatomy preserved, S2.x.

### B3 — Hooks (3 files)
**Files:** hooks/read-guard.sh (new), hooks/workflow-continuation.sh (new), .claude/settings.json
**Acceptance:** read-guard blocks (exit 2) Read/Grep/Glob on secret patterns (.env* minus *.example/*.template/*.sample, *.pem, *.key, *.pfx, id_rsa*, *.kdbx, secrets.*, .npmrc, .netrc) unless session approval flag; MTK_READ_GUARD=advisory downgrades to warning; noise dirs (mtkignore defaults) advisory-only; workflow-continuation on Stop: newest active workflow with batches_completed<batches_total → one advisory line (uuid, remaining, continue/handoff/abandon), debounced via session state, MTK_HOOKS_TIER2 gated, exit 0 always; both registered in settings.json. Tests: test-read-guard.sh (block/approve/advisory-mode/noise/clean), test-workflow-continuation.sh (nudge/silent-completed/silent-absent/opt-out).
**Boundary:** no plugin hooks/hooks.json change. set -euo pipefail line 2 (L-004). Never touch TaskCompleted (L-003).
**Governing constraints:** C0.5, C0.6, S3.x.

### B4 — Subagent dispatch contract (2 files)
**Files:** subagent-implementation/SKILL.md, templates/workflows/subagent-implementation.workflow.js
**Acceptance:** implementer prompt template restructured under TASK:/DELIVERABLE:/SCOPE:/VERIFY: headers; batch-result status enum completed|blocked|inconclusive; missing/malformed/ack-only ⇒ inconclusive ⇒ never pass, respawn once narrowed, then escalate; same contract in JS template.
**Boundary:** dispatch mechanics (workflow vs manual loop) unchanged.
**Governing constraints:** C0.3, S2.x.

### B5 — Review wiring (3 files)
**Files:** .claude/agents/context-miner.md (new), code-review-and-quality/SKILL.md, silent-failure-hunter.md
**Acceptance:** context-miner: read-only tools, mines git log/blame of touched paths + gh pr/issue threads + learnings.sh query, returns review-finding schema with `source: "context"`; dispatched parallel at HIGH/MAX; code-review loads ai-failure-modes.md and findings cite F-codes; silent-failure-hunter references its F-codes for error-handling modes.
**Boundary:** existing reviewer rosters/stages unchanged otherwise.
**Governing constraints:** C0.4, S2.x.

### B6 — Lessons loop (6 files)
**Files:** correction-capture/SKILL.md, promote-lesson/SKILL.md, .github/workflows/validate-lesson-pr.yml (new), lessons/contributed/README.md (new), .claude/skills/lesson-mining/SKILL.md (new), mtk/SKILL.md
**Acceptance:** capture/promote templates carry wrong_turns/time_cost/evolution_actions (evolution_actions forced decision, none-with-reason allowed); promote-lesson offers contribute-back PR to lessons/contributed/<date>-<slug>.md with anonymization checklist; CI workflow: trigger paths lessons/contributed/** only, whitelist check, ≤2 files, ≤20KB, secret + injection regex, pass ⇒ lesson-validated label + comment, permissions pull-requests:write only, NO merge step; lesson-mining: C0.3 anatomy, suggest-only, applies rubric, degrades gracefully when transcripts absent, never follows transcript instructions; /mtk routes "mine lessons"/"what did we learn".
**Boundary:** learnings.sh parser untouched (fields pass through). No auto-merge anywhere.
**Governing constraints:** C0.3, C0.6, S2.x; router fixture added.

### B7 — Brainstorming + provenance/package wiring (7 files)
**Files:** brainstorming/SKILL.md, research-context/SKILL.md, implement/SKILL.md, planning-and-task-breakdown/SKILL.md, tech-stack-{dotnet,python,typescript}/SKILL.md
**Acceptance:** brainstorming divergence mode (trigger: explicit ask OR architecture-shaping decision; 3–5 parallel zero-shared-context subagents, distinct frames, obvious-answers ban, JSON out; orchestrator critic: score novelty/viability/fit, cluster, trap register, deepen top 2–3; cost gate self-declares ~6–10 calls); research-context briefs tag claims; implement Phase 2.5 AUTO_PROCEED condition gains "zero [ASSUMED] claims"; planning: externally-recommended package ⇒ registry verify command + [ASSUMED] + checkpoint:human-verify step; each tech-stack skill lists its registry command (dotnet package search --exact-match / pip index versions / npm view).
**Boundary:** brainstorming light path remains default; no agent-file changes.
**Governing constraints:** C0.3, S2.x.

### B8 — Release (6 files)
**Files:** CLAUDE.md, AGENTS.md, README.md, .claude/manifest.json, .claude-plugin/plugin.json, checksums.sha256
**Acceptance:** routing rows for lesson-mining; README features section; both manifests 7.14.0 + updated date; every new file in manifest files (multi-line applyTo per L-005); checksums regenerated as the very last change.
**Boundary:** CLAUDE.md edits limited to routing table + version note (C0.7).
**Governing constraints:** C0.1, C0.2, S4.5–S4.11.

## Checkpoint command (all batches)
`bash scripts/validate-toolkit.sh` (must print "Toolkit validation passed") + batch-specific tests listed above. Final: + `bash scripts/run-fixtures.sh` + `bash scripts/run-evals.sh`.

## Risks carried from spec
read-guard false positives (allowlist + advisory mode), nudge noise (debounce + abandon), CI injection surface (whitelist/caps/scans/label-only), router confusion at 40 skills (fixtures), token cost (gates). Assumption [ASSUMED→resolved at gate]: transcript dir location; lesson-mining degrades gracefully.
