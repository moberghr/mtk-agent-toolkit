# Plan: v7.5.0 — SpecOps Borrows

Companion to `docs/specs/2026-05-08-v7.5.0-specops-borrows.md`. Six batches, ~350–500 LOC of markdown/bash total.

## Batch 1 — Infrastructure & Schemas
**Files (new):**
- `.claude/references/dependency-intake-checklist.md` — 5 criteria with rubric
- `.claude/references/learnings-schema.md` — JSON schema for `.mtk/learnings.json` entries
- `scripts/learnings.sh` — `add | query | regen-markdown | migrate` subcommands
- `scripts/lint-ears.sh` — structural lint over `## Requirements` sections

**Checkpoint:** `bash scripts/lint-ears.sh docs/specs/2026-05-08-v7.5.0-specops-borrows.md` returns 0; `bash scripts/learnings.sh add --dry-run` round-trips a fixture entry.

## Batch 2 — EARS + ANT
**Files (edit):**
- `.claude/skills/spec-driven-development/SKILL.md` — append "Requirements Format (EARS)" section + ANT step in workflow
- `.claude/skills/spec-drift-detection/SKILL.md` — call `lint-ears.sh` against the spec being checked

**Checkpoint:** `lint-ears.sh` flags a deliberately bad fixture; passes on the v7.5.0 spec.

## Batch 3 — Scored Adversarial Evaluator
**Files (edit):**
- `.claude/agents/compliance-reviewer.md` — scoring contract (5 dims), evidence quotes, auto-fail rules, JSON emission to workflow artifact
- `.claude/skills/code-review-and-quality/SKILL.md` — same contract, 2-iter cap, escalation rule
- `.claude/references/review-finding-schema.md` — extend schema with `scores: {correctness, security, test_coverage, architecture_fit, simplicity}` block

**Checkpoint:** schema example parses; smoke-test review prompt produces all 5 scores.

## Batch 4 — Dependency-Introduction Gate
**Files (edit):**
- `.claude/skills/pre-commit-review/SKILL.md` — detect dep-manifest changes, invoke checklist, two Poors → block
- `.claude/skills/security-and-hardening/SKILL.md` — reference checklist as part of supply-chain scope

**Checkpoint:** simulated `git diff` showing `package.json` change triggers checklist instruction in skill output.

## Batch 5 — Structured Learnings
**Files (edit):**
- `.claude/skills/correction-capture/SKILL.md` — call `learnings.sh add`, regen markdown view
- `.claude/skills/promote-lesson/SKILL.md` — set `scope: team` on entry; regen
- `.claude/skills/fix/SKILL.md` — call `learnings.sh query` at start (5-layer filter)
- `tasks/lessons.md` — migrate to auto-generated section + manual section divider

**Checkpoint:** `learnings.sh migrate` produces `.mtk/learnings.json` with all existing lessons; `regen-markdown` reproduces `tasks/lessons.md` content.

## Batch 6 — Manifest, Validate, Release Notes
**Files (edit):**
- `.claude/manifest.json` — version 7.5.0, updated date, new file entries, applyTo globs
- `.claude-plugin/plugin.json` — version 7.5.0
- `tasks/lessons.md` — final pass after migration

**Checkpoint:** `bash scripts/validate-toolkit.sh` passes. Both versions match.

## Review Plan

After all 6 batches:
- Stage 1: `compliance-reviewer` against the full diff with scoring (dogfood new contract).
- Stage 2: `architecture-reviewer` (new boundaries: `.mtk/learnings.json` + scripts) + `test-reviewer` (pressure tests).

## Rollback

Each batch is its own commit on `feat/v7.5.0-specops-borrows`. Revert is per-batch. No DB migrations or external systems touched.
