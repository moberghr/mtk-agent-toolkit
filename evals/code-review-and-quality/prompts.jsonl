{"id": "cr-fp-pre-existing", "prompt": "Review this diff. Note that line 18 (the unchanged context line) has a SQL string concatenation; the diff itself only adds logging:\n\n```diff\n@@ -15,6 +15,9 @@ public class UserService\n     public User Find(string name)\n     {\n-        return db.ExecuteRaw($\"SELECT * FROM users WHERE name = '{name}'\");\n+        _logger.LogInformation(\"Looking up user {Name}\", name);\n+        return db.ExecuteRaw($\"SELECT * FROM users WHERE name = '{name}'\");\n     }\n```", "assertion": "The response must NOT flag the SQL injection on the unchanged context line as a finding for this PR. It is pre-existing and outside the diff (FP exclusion category 1). The response may mention it as out-of-scope context.", "expected_grade": "pass"}
{"id": "cr-confidence-threshold", "prompt": "Review this small diff for the staged changes:\n\n```diff\n+ // TODO: validate inputs later\n+ public Result Transfer(decimal amount, Guid from, Guid to) {\n+     _ledger.Move(from, to, amount);\n+     return Result.Ok();\n+ }\n```", "assertion": "The response must surface a Critical-severity finding about missing input validation and missing audit logging on a money-mutation method. Confidence on at least one finding must be ≥ 80.", "expected_grade": "pass"}
{"id": "cr-clean-diff-rationale", "prompt": "Review this diff:\n\n```diff\n+ public string FormatBytes(long n)\n+ {\n+     if (n < 1024) return $\"{n} B\";\n+     return $\"{n / 1024} KB\";\n+ }\n```", "assertion": "The response must EITHER report fewer than 2 findings WITH a populated below_threshold_rationale citing which axes were checked, OR report findings with confidence ≥ 80. It must NOT manufacture findings just to fill space (no anti-sandbagging).", "expected_grade": "pass"}
{"id": "cr-linter-catchable-skip", "prompt": "Review this diff:\n\n```diff\n+ const x: any = getThing()\n+ console.log(x.value)\n```", "assertion": "The response must NOT flag 'using any defeats the type system' as a finding from the AI source — that is a typechecker-catchable issue (FP exclusion category 2). It may flag the broader pattern (e.g., untyped boundary) only if a concrete vector is named.", "expected_grade": "pass"}
{"id": "cr-justified-silence", "prompt": "Review this diff:\n\n```diff\n+ // eslint-disable-next-line no-explicit-any -- third-party SDK has incorrect types; tracked DEVOPS-4421\n+ const handler: any = sdk.makeHandler()\n```", "assertion": "The response must NOT flag the eslint-disable as a finding. The silence is explicitly justified inline with a tracker reference (FP exclusion category 4).", "expected_grade": "pass"}
{"id": "cr-output-schema", "prompt": "Review this diff:\n\n```diff\n+ public Task<int> CountAsync() => _db.Users.CountAsync();\n```", "assertion": "Regardless of how many findings are surfaced, the response must include both a markdown table AND a fenced JSON block conforming to the review-finding-schema (verdict, summary, findings array, threshold).", "expected_grade": "pass"}
{"id": "cr-error-handling-dispatch-trigger", "prompt": "Review this diff:\n\n```diff\n+ try {\n+     await _ledger.RecordSettlement(paymentId);\n+ } catch (Exception) {\n+ }\n+ return Result.Ok();\n```", "assertion": "The response must surface at least one Critical-severity finding about the empty catch swallowing failures in an audited (settlement/ledger) path. Confidence ≥ 85.", "expected_grade": "pass"}
